@model IEnumerable<EmpDetails.Models.Employee>

@{
    ViewBag.Title = "Index";
    
    
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<head>
<style>
table {
  border-collapse: collapse;
}

table, td, th {
  border: 2px solid black;
  
}
    th {
        color: red;
    }
</style>
</head>
 @using (Html.BeginForm("Index1", "Operations", FormMethod.Post))
    {
        <span>Customer Name:</span> @Html.TextBox("Name")
        <input type="submit" value="Search"/>
 }
<table style="width:100%">

    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
     
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Dept.Department_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmpRole.Role_Name)
        </th>
 
        <th></th>

    </tr>
       </div>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Dept.Department_Name)
        </td>
         <td>
            @Html.DisplayFor(model => item.EmpRole.Role_Name)
        </td>
     
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })

        </td>
    </tr>
}

</table>
            @Html.ActionLink("LogOut","Logout","Authorize", Session["user"] = null)
